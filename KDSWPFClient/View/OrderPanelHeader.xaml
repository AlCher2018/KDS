<UserControl x:Class="KDSWPFClient.View.OrderPanelHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KDSWPFClient.View"
             mc:Ignorable="d" 
             Name="root"
             d:DesignHeight="100" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}"
             >

    <UserControl.Resources>
        <Style x:Key="rowMarginStyle" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="{Binding ActualWidth, ConverterParameter='0.02,0,0.02,0', Converter={StaticResource getMargin}, ElementName=root, Mode=OneWay}"/>
        </Style>
        
        <!-- стили закругленных углов в рамках -->
        <Style x:Key="cornRadAllStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="{Binding ActualWidth, ConverterParameter=0.025, Converter={StaticResource getCornerRadius}, ElementName=root, Mode=OneWay}"/>
        </Style>
        <Style x:Key="cornRadTopStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="{Binding ActualWidth, ConverterParameter='0.03,0.03,0,0', Converter={StaticResource getCornerRadius}, ElementName=root, Mode=OneWay}"/>
        </Style>
        <Style x:Key="cornRadBottomStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="{Binding ActualWidth, ConverterParameter='0,0,0.03,0.03', Converter={StaticResource getCornerRadius}, ElementName=root, Mode=OneWay}"/>
        </Style>
    </UserControl.Resources>
    
    <!-- строка заголовка, содержит 3 строки -->
    <Grid x:Name="grdHeader" Background="White">
        <Grid.RowDefinitions>
            <!-- 0. наимен.стола и номер заказа -->
            <RowDefinition Height="1.2*"/>
            <!-- 1. имя официанта -->
            <RowDefinition Height="0.8*"/>
            <!-- 2. таймер состояния заказа -->
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!-- строка с наимен.стола и номером заказа -->
        <Border x:Name="brdHrdTableRow" Style="{StaticResource cornRadTopStyle}" Background="Yellow">
            <Grid x:Name="grdHdrTableRow" Grid.Row="0" Style="{StaticResource rowMarginStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock x:Name="tbTableLabel" Text="Стол № " VerticalAlignment="Center"/>
                    <TextBlock x:Name="tbTableName" FontWeight="Bold" VerticalAlignment="Center" Text="{Binding TableName, ElementName=root}"/>
                </StackPanel>
                <TextBlock x:Name="tbOrderNumber" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Text="{Binding ElementName=root, Path=OrderNumber}"/>
            </Grid>
        </Border>
        
        <!-- официант -->
        <Grid x:Name="grdHdrWaiter" Grid.Row="1" Style="{StaticResource rowMarginStyle}" Background="Yellow">
            <TextBlock x:Name="tbWaiter" TextWrapping="Wrap" Text="{Binding ElementName=root, Path=WaiterName}"/>
        </Grid>
        
        <!-- время создания заказа и счетчик приготовления -->
        <Grid x:Name="grdHdrOrderTime" Grid.Row="2" Style="{StaticResource rowMarginStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <TextBlock x:Name="tbOrderDateLabel" Text="Создан в: " VerticalAlignment="Center"/>
                <TextBlock x:Name="tbOrderDate" FontWeight="Bold" VerticalAlignment="Center" Text="{Binding ElementName=root, Path=CreateDate, Mode=OneWay, TargetNullValue='noDate', StringFormat={}{0:HH:mm:ss}}"/>
            </StackPanel>
            <Border Grid.Column="1" Padding="5,3" Margin="0,3" Style="{StaticResource cornRadAllStyle}" Background="Aqua">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="tbOrderCookingCounterLabel" Text="Прошло: " VerticalAlignment="Center"/>
                    <TextBlock x:Name="tbOrderCookingCounter" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
        
    </Grid>
</UserControl>
