<UserControl x:Class="KDSWPFClient.View.OrderPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:core="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:KDSWPFClient"
             mc:Ignorable="d"
             d:DesignHeight="20" d:DesignWidth="300">

    <Grid >
        <Grid.RowDefinitions>
            <!-- 0. строка заголовка заказа, может содержать OrderPanelHeader-->
            <RowDefinition Height="Auto"/>
            <!-- 1. заголовок таблицы блюд -->
            <RowDefinition Height="Auto"/>
            <!-- 2. строка блюд -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="grdHeader" x:FieldModifier="private" Grid.Row="0"/>

        <!-- заголовок таблицы блюд -->
        <Border Grid.Row="1" Background="AliceBlue" BorderBrush="DarkGray" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <!-- 0. № п/п -->
                    <ColumnDefinition Width="0.2*"/>
                    <!-- 1. номер подачи -->
                    <ColumnDefinition Width="0.2*"/>
                    <!-- 2. наименование блюда -->
                    <ColumnDefinition Width="*"/>
                    <!-- 3. количество -->
                    <ColumnDefinition Width="0.3*"/>
                    <!-- 4. таймер состояния -->
                    <ColumnDefinition Width="0.6*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="№п/п" Grid.Column="0" VerticalAlignment="Center"/>
                <TextBlock Text="Подача" Grid.Column="1" VerticalAlignment="Center"/>
                <TextBlock Text="Блюдо" Grid.Column="2" VerticalAlignment="Center"/>
                <TextBlock Text="Кол-во" Grid.Column="3" VerticalAlignment="Center"/>
                <TextBlock Text="Время" Grid.Column="4" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <StackPanel x:Name="stkDishes" x:FieldModifier="private" Grid.Row="2"/>

    </Grid>
</UserControl>
