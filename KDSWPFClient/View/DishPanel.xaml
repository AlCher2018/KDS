<UserControl x:Class="KDSWPFClient.View.DishPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KDSWPFClient.View"
             mc:Ignorable="d" Name="root"
             d:DesignHeight="60" d:DesignWidth="300">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border x:Name="brdMain" Background="White" BorderBrush="DarkBlue" BorderThickness="1,0,1,1">
        <Grid x:Name="grdDishLine">
            <Grid.ColumnDefinitions>
                <!-- 0. № п/п -->
                <ColumnDefinition Width="0.2*"/>
                <!-- 1. номер подачи -->
                <ColumnDefinition Width="0.2*"/>
                <!-- 2. наименование блюда -->
                <ColumnDefinition Width="*"/>
                <!-- 3. количество -->
                <ColumnDefinition Width="0.3*"/>
                <!-- 4. таймер состояния -->
                <ColumnDefinition Width="0.6*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="tbDishIndex" Grid.Column="0" TextAlignment="Center"/>
            <TextBlock x:Name="tbDishFilingNumber" Grid.Column="1"/>
            <TextBlock x:Name="tbDishName" Grid.Column="2" TextWrapping="Wrap"/>
            <TextBlock x:Name="tbDishQuantity" Grid.Column="3"/>
            <TextBlock x:Name="tbDishStatusTS" Grid.Column="4"/>

        </Grid>
    </Border>
    
</UserControl>
